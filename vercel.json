{
  "buildCommand": "npm run build && cp public/admin.html dist/admin.html && cd admin-app && npm install && npm run build && cp dist/admin.html ../dist/admin.html && cp -r dist/assets/* ../dist/assets/ && cd ..",
  "outputDirectory": "dist",
  "framework": null,
  "installCommand": "npm install",
  "functions": {},
  "rewrites": [
    {
      "source": "/admin/(.*)",
      "destination": "/admin.html"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "env": {
    "NODE_ENV": "production"
  }
}
